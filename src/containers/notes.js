// takes the component into the container making a new container then exporting it to an index and then using that for the overall UI
// want to use our componenets over and over again
// if the data is wrapped inside the container, then we can't use that

// UserContext can make something available in a different state or form